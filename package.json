{
  "name": "team-task-manager-app",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm run build --prefix frontend && npm run vercel-build --prefix backend"
  }
}